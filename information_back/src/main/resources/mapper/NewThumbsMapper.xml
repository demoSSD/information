<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heeexy.example.dao.NewThumbsDao">
    <insert id="addThumb" parameterType="com.alibaba.fastjson.JSONObject">
        insert into thumbs(user_id, question_id,question_user,type) values (#{userId},#{questionId},#{questionUser},#{type})
    </insert>
    <select id="exits" resultType="int">
        select count(*) from thumbs where user_id=#{userId} and question_id=#{questionId}
    </select>
    <update id="updateThumb" >
        UPDATE thumbs SET type=#{type},question_user=#{questionUser},delete_status=1 where user_id=#{userId} and question_id=#{questionId}
    </update>
    <update id="deleteThumb" >
        UPDATE thumbs SET delete_status=2 where user_id=#{userId} and question_id=#{questionId}
    </update>
</mapper>

